require 'van'
require 'docking_station'

describe Van do
  # it { is_expected.to respond_to(:collect_broken_bikes) }
  #
  # it 'Van should collect all broken bikes from docking station' do
  #   van = Van.new
  #   station = DockingStation.new
  #   # working_bikes = [2.times { station.dock_bike double(:bike) }]
  #   broken_bikes = [2.times { station.dock_bike(double(:bike, broken?: true)) } ]
  #   expect(van.bikes).to include(broken_bike)
  # end

end
